# Build
The Build repository contains the scripts to compile all projects and to create a new package.

  * **Install git tools** for windows 
  * Make sure the **bin/ path** is added to the **PATH environment variable**
  * **Clone the Build repo** to your Windows system and change into the newly created **Build** directory
  * In the DOS box execute the script **FetchRepos.bat**
